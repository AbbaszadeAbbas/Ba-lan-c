// function Kvadratlar(_kvadratlarinSayi, _sutun, _gap, _with_box, _height_box, _common_width) {let common = document.createElement("div");common.style.gridTemplateColumns = `repeat(${_sutun}, 1fr)`;common.style.gap = `${_gap}px`;common.style.width = `${_common_width}px`;common.style.marginTop = "0";common.style.marginLeft = "auto";common.style.marginRight = "auto";common.style.display = "grid";for (i = 0; i < _kvadratlarinSayi; i++) {let RandR = Math.floor(Math.random() * 255);let RandG = Math.floor(Math.random() * 255);let RandB = Math.floor(Math.random() * 255);let box = document.createElement("div");box.style.background = `rgb(${RandR},${RandG},${RandB})`;box.style.width = `${_with_box}px`;box.style.height = `${_height_box}px`;common.appendChild(box)}document.body.appendChild(common)}Kvadratlar(15, 3, 20, 387, 200, 1200)
// function Foo(){for (let i = 0; i < 100;i++) {let p = document.createElement("p");p.innerHTML = `${i}-ci eded`;document.body.appendChild(p)};}Foo()